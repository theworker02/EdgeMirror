# DEMO screenshot dataset

**This directory is DEMO-only.** Do not treat files here as production evidence or live Cloudflare results.

## Contents

| Path | Purpose |
|------|---------|
| `dataset.json` | Deterministic dashboard / screenshot fixture |
| `report.terminal.txt` | Captured-style CLI report (DEMO) |
| `README.DEMO.md` | This file |

Generate a fresh isolated DEMO via:

```bash
npx edgemirror demo --keep
```

Finding IDs use the `EM-DEMO-*` prefix. Product status overlay: `DEMO`.
